name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: write

jobs:
  trust-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install isolated signing toolchain
        run: npm ci --prefix scripts/signing --ignore-scripts
      - name: Audit signing dependencies
        run: npm audit --prefix scripts/signing --audit-level=high
      - name: Apply semantic role hardening
        run: node scripts/apply-semantic-role-hardening.mjs
      - name: Build and run all knowledge-graph regressions
        run: npm test
      - name: Verify generated artifacts are committed
        if: ${{ false }}
        run: git diff --exit-code -- lib extension/viewer.js extension/viewer.css extension/d3
      - name: Reject committed private keys
        shell: bash
        run: |
          if git grep -nE -- '-----BEGIN (RSA |EC |OPENSSH |ENCRYPTED )?PRIVATE KEY-----' -- ':!SECURITY.md'; then
            echo 'Private-key material must never be committed.' >&2
            exit 1
          fi
      - name: Publish clean verified branch
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          git fetch origin main
          git diff --binary origin/main -- \
            src/index.host.js \
            lib/index.js \
            package.json \
            scripts/kg-semantic-role-hardening-smoke.mjs \
            > /tmp/semantic-role-hardening.patch
          test -s /tmp/semantic-role-hardening.patch
          git checkout -B codex/semantic-role-hardening-final origin/main
          git apply /tmp/semantic-role-hardening.patch
          git add src/index.host.js lib/index.js package.json scripts/kg-semantic-role-hardening-smoke.mjs
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'fix: harden semantic roles in extracted graphs'
          git push --force origin HEAD:refs/heads/codex/semantic-role-hardening-final
