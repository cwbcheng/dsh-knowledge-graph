import { readFileSync, writeFileSync } from 'node:fs'

function replaceOnce(text, before, after, label) {
  if (!text.includes(before)) throw new Error(label + ' marker not found')
  const next = text.replace(before, after)
  if (next === text) throw new Error(label + ' replacement did not change content')
  return next
}

// ---- Host: unique, typography-only quote rebind ----
const hostPath = 'src/index.host.js'
let host = readFileSync(hostPath, 'utf8')

const groundingMarker = `      function provenanceForParagraphHost(graph, paragraph) {\n`
const groundingHelper = `      function foldEvidenceTypographyHost(value) {\n        const source = String(value || '')\n        const chars = []\n        const map = []\n        let lastWasSpace = true\n        const doubleQuotes = new Set(['"', '“', '”', '„', '‟', '「', '」', '『', '』'])\n        const singleQuotes = new Set(["'", '‘', '’', '‚', '‛'])\n        for (let i = 0; i < source.length; i++) {\n          const raw = source[i]\n          let ch = raw\n          if (doubleQuotes.has(raw)) ch = '"'\n          else if (singleQuotes.has(raw)) ch = "'"\n          else if (/\\s/.test(raw) || raw === IDEO_SPACE_HOST) ch = ' '\n          if (ch === ' ') {\n            if (lastWasSpace) continue\n            chars.push(' ')\n            map.push(i)\n            lastWasSpace = true\n            continue\n          }\n          chars.push(ch)\n          map.push(i)\n          lastWasSpace = false\n        }\n        if (chars.length > 0 && chars[chars.length - 1] === ' ') { chars.pop(); map.pop() }\n        return { text: chars.join(''), map }\n      }\n      function exactOrUniqueTypographicQuoteHost(sourceText, rawQuote) {\n        const source = String(sourceText || '')\n        const quote = String(rawQuote || '').trim().slice(0, 600)\n        if (!source || !quote) return ''\n        if (source.includes(quote)) return quote\n        const foldedSource = foldEvidenceTypographyHost(source)\n        const foldedQuote = foldEvidenceTypographyHost(quote).text\n        if (!foldedQuote) return ''\n        const first = foldedSource.text.indexOf(foldedQuote)\n        if (first < 0 || foldedSource.text.indexOf(foldedQuote, first + 1) >= 0) return ''\n        const last = first + foldedQuote.length - 1\n        const startOffset = foldedSource.map[first]\n        const endOffset = foldedSource.map[last]\n        if (!Number.isInteger(startOffset) || !Number.isInteger(endOffset)) return ''\n        return source.slice(startOffset, endOffset + 1).trim()\n      }\n\n`
if (!host.includes(groundingMarker)) throw new Error('grounding helper insertion marker not found')
host = host.replace(groundingMarker, groundingHelper + groundingMarker)

const oldAuth = `      function authenticateGraphEvidenceHost(graph, sourceText) {\n        if (!graph || typeof graph !== 'object') return graph\n        const paragraphs = splitParagraphsHost(typeof sourceText === 'string' ? sourceText : '')\n        const quoteMatches = (paragraph, quote) => {\n          if (!Number.isInteger(paragraph) || paragraph < 0 || paragraph >= paragraphs.length || !quote) return false\n          const source = String(paragraphs[paragraph] || '')\n          const normalizedSource = source.replace(/\\s+/g, ' ').trim()\n          const normalizedQuote = String(quote).replace(/\\s+/g, ' ').trim()\n          return source.includes(quote) || Boolean(normalizedQuote && normalizedSource.includes(normalizedQuote))\n        }\n        for (const node of Array.isArray(graph.nodes) ? graph.nodes : []) {\n          if (!node || typeof node !== 'object') continue\n          const canonicalProvenance = provenanceForParagraphHost(graph, node.paragraph)\n          if (canonicalProvenance.documentId) node.documentId = canonicalProvenance.documentId\n          if (canonicalProvenance.sourceId) node.sourceId = canonicalProvenance.sourceId\n          if (canonicalProvenance.chunkId) node.chunkId = canonicalProvenance.chunkId\n          const authenticated = []\n          for (const item of Array.isArray(node.evidence) ? node.evidence : []) {\n            if (!item || !Number.isInteger(item.paragraph) || typeof item.quote !== 'string' || !item.quote.trim()) continue\n            const quote = item.quote.trim().slice(0, 600)\n            if (!quoteMatches(item.paragraph, quote)) continue\n            authenticated.push(evidenceRecordHost(item.paragraph, quote, provenanceForParagraphHost(graph, item.paragraph), item))\n          }\n          const quote = typeof node.quote === 'string' ? node.quote.trim().slice(0, 600) : ''\n          if (quote && Number.isInteger(node.paragraph) && quoteMatches(node.paragraph, quote)) {\n            authenticated.push(evidenceRecordHost(node.paragraph, quote, provenanceForParagraphHost(graph, node.paragraph), null))\n          }\n          node.evidence = mergeEvidenceRecordsHost([], authenticated, 8)\n          refreshNodeGroundingStatusHost(node)\n        }\n        for (const edge of Array.isArray(graph.edges) ? graph.edges : []) {\n          if (!edge || typeof edge !== 'object') continue\n          const authenticated = []\n          for (const item of Array.isArray(edge.evidence) ? edge.evidence : []) {\n            if (!item || !Number.isInteger(item.paragraph) || typeof item.quote !== 'string' || !item.quote.trim()) continue\n            const quote = item.quote.trim().slice(0, 600)\n            if (!quoteMatches(item.paragraph, quote)) continue\n            authenticated.push(evidenceRecordHost(item.paragraph, quote, provenanceForParagraphHost(graph, item.paragraph), item))\n          }\n          edge.evidence = mergeEvidenceRecordsHost([], authenticated, 8)\n        }\n        return graph\n      }`
const newAuth = `      function authenticateGraphEvidenceHost(graph, sourceText) {\n        if (!graph || typeof graph !== 'object') return graph\n        const paragraphs = splitParagraphsHost(typeof sourceText === 'string' ? sourceText : '')\n        const quoteMatch = (paragraph, quote) => {\n          if (!Number.isInteger(paragraph) || paragraph < 0 || paragraph >= paragraphs.length || !quote) return ''\n          return exactOrUniqueTypographicQuoteHost(String(paragraphs[paragraph] || ''), quote)\n        }\n        for (const node of Array.isArray(graph.nodes) ? graph.nodes : []) {\n          if (!node || typeof node !== 'object') continue\n          const canonicalProvenance = provenanceForParagraphHost(graph, node.paragraph)\n          if (canonicalProvenance.documentId) node.documentId = canonicalProvenance.documentId\n          if (canonicalProvenance.sourceId) node.sourceId = canonicalProvenance.sourceId\n          if (canonicalProvenance.chunkId) node.chunkId = canonicalProvenance.chunkId\n          const authenticated = []\n          for (const item of Array.isArray(node.evidence) ? node.evidence : []) {\n            if (!item || !Number.isInteger(item.paragraph) || typeof item.quote !== 'string' || !item.quote.trim()) continue\n            const matchedQuote = quoteMatch(item.paragraph, item.quote.trim().slice(0, 600))\n            if (!matchedQuote) continue\n            authenticated.push(evidenceRecordHost(item.paragraph, matchedQuote, provenanceForParagraphHost(graph, item.paragraph), item))\n          }\n          const quote = typeof node.quote === 'string' ? node.quote.trim().slice(0, 600) : ''\n          if (quote && Number.isInteger(node.paragraph)) {\n            const matchedQuote = quoteMatch(node.paragraph, quote)\n            if (matchedQuote) {\n              node.quote = matchedQuote\n              authenticated.push(evidenceRecordHost(node.paragraph, matchedQuote, provenanceForParagraphHost(graph, node.paragraph), null))\n            }\n          }\n          node.evidence = mergeEvidenceRecordsHost([], authenticated, 8)\n          refreshNodeGroundingStatusHost(node)\n        }\n        for (const edge of Array.isArray(graph.edges) ? graph.edges : []) {\n          if (!edge || typeof edge !== 'object') continue\n          const authenticated = []\n          for (const item of Array.isArray(edge.evidence) ? edge.evidence : []) {\n            if (!item || !Number.isInteger(item.paragraph) || typeof item.quote !== 'string' || !item.quote.trim()) continue\n            const matchedQuote = quoteMatch(item.paragraph, item.quote.trim().slice(0, 600))\n            if (!matchedQuote) continue\n            authenticated.push(evidenceRecordHost(item.paragraph, matchedQuote, provenanceForParagraphHost(graph, item.paragraph), item))\n          }\n          edge.evidence = mergeEvidenceRecordsHost([], authenticated, 8)\n        }\n        return graph\n      }`
host = replaceOnce(host, oldAuth, newAuth, 'final evidence authentication')

const oldQuoteInParagraph = `        const quoteInParagraph = (quote, paragraph) => {\n          if (!quote || !Number.isInteger(paragraph) || paragraph < 0 || paragraph >= paragraphTexts.length) return false\n          const source = String(paragraphTexts[paragraph] || '')\n          const normalizedSource = source.replace(/\\s+/g, ' ').trim()\n          const normalizedQuote = String(quote).replace(/\\s+/g, ' ').trim()\n          return source.includes(quote) || (normalizedQuote && normalizedSource.includes(normalizedQuote))\n        }`
const newQuoteInParagraph = `        const quoteInParagraph = (quote, paragraph) => {\n          if (!quote || !Number.isInteger(paragraph) || paragraph < 0 || paragraph >= paragraphTexts.length) return false\n          return Boolean(exactOrUniqueTypographicQuoteHost(String(paragraphTexts[paragraph] || ''), quote))\n        }`
host = replaceOnce(host, oldQuoteInParagraph, newQuoteInParagraph, 'invariant quote matching')

const oldRelationEvidence = `          if (paragraphs && typeof paragraphs[paragraph] === 'string') {\n            const source = paragraphs[paragraph]\n            const normalizedSource = source.replace(/\\s+/g, ' ').trim()\n            const normalizedQuote = quote.replace(/\\s+/g, ' ').trim()\n            if (!source.includes(quote) && !normalizedSource.includes(normalizedQuote)) continue\n          }\n          out.push(evidenceRecordHost(paragraph, quote, sourceContext, item))`
const newRelationEvidence = `          let authenticatedQuote = quote\n          if (paragraphs && typeof paragraphs[paragraph] === 'string') {\n            authenticatedQuote = exactOrUniqueTypographicQuoteHost(paragraphs[paragraph], quote)\n            if (!authenticatedQuote) continue\n          }\n          out.push(evidenceRecordHost(paragraph, authenticatedQuote, sourceContext, item))`
host = replaceOnce(host, oldRelationEvidence, newRelationEvidence, 'relation evidence typography rebind')

const oldNodeEvidence = `          const sourceParagraph = paragraphs && pNum != null && typeof paragraphs[pNum] === 'string' ? paragraphs[pNum] : ''\n          const normalizedSource = sourceParagraph.replace(/\\s+/g, ' ').trim()\n          const normalizedQuote = quote.replace(/\\s+/g, ' ').trim()\n          const quoteAuthenticated = Boolean(quote && pNum != null && sourceParagraph && (sourceParagraph.includes(quote) || (normalizedQuote && normalizedSource.includes(normalizedQuote))))\n          const evidence = quoteAuthenticated ? [evidenceRecordHost(pNum, quote, sourceContext, null)] : []`
const newNodeEvidence = `          const sourceParagraph = paragraphs && pNum != null && typeof paragraphs[pNum] === 'string' ? paragraphs[pNum] : ''\n          const authenticatedQuote = quote && pNum != null && sourceParagraph ? exactOrUniqueTypographicQuoteHost(sourceParagraph, quote) : ''\n          const quoteAuthenticated = Boolean(authenticatedQuote)\n          const evidence = quoteAuthenticated ? [evidenceRecordHost(pNum, authenticatedQuote, sourceContext, null)] : []`
host = replaceOnce(host, oldNodeEvidence, newNodeEvidence, 'node evidence typography rebind')
writeFileSync(hostPath, host)

// ---- QA benchmark: semantic + trusted views; primary score is trusted ----
const benchmarkPath = 'scripts/kg-qa-benchmark.mjs'
let benchmark = readFileSync(benchmarkPath, 'utf8')
const oldRun = `export function runGraphQaBenchmark(graph, cases) {\n  if (!graph || !Array.isArray(graph.nodes) || !Array.isArray(graph.edges)) throw new Error('graph must contain nodes[] and edges[]')\n  if (!Array.isArray(cases) || cases.length === 0) throw new Error('cases must be a non-empty array')\n  const results = cases.map((testCase) => ({ id: testCase.id, question: testCase.question, category: testCase.category || 'answerability', ...evaluateCase(graph, testCase) }))\n  const categories = {}\n  for (const result of results) {\n    const entry = categories[result.category] || { passed: 0, total: 0 }\n    entry.total += 1\n    if (result.pass) entry.passed += 1\n    categories[result.category] = entry\n  }\n  const passed = results.filter((r) => r.pass).length\n  return {\n    ok: passed === results.length,\n    passed,\n    total: results.length,\n    score: Math.round((passed / results.length) * 100),\n    categories,\n    results,\n  }\n}`
const newRun = `function evaluateCases(graph, cases) {\n  return cases.map((testCase) => ({ id: testCase.id, question: testCase.question, category: testCase.category || 'answerability', ...evaluateCase(graph, testCase) }))\n}\n\nfunction aggregateResults(results) {\n  const categories = {}\n  for (const result of results) {\n    const entry = categories[result.category] || { passed: 0, total: 0 }\n    entry.total += 1\n    if (result.pass) entry.passed += 1\n    categories[result.category] = entry\n  }\n  const passed = results.filter((result) => result.pass).length\n  return { passed, total: results.length, score: Math.round((passed / results.length) * 100), categories }\n}\n\nfunction withoutUnsupportedNodes(graph) {\n  const nodes = (graph.nodes || []).filter((node) => node && node.groundingStatus !== 'unsupported')\n  const ids = new Set(nodes.map((node) => node.id))\n  const edges = (graph.edges || []).filter((edge) => edge && ids.has(edge.fromNodeId) && ids.has(edge.toNodeId))\n  return { ...graph, nodes, edges }\n}\n\nexport function runGraphQaBenchmark(graph, cases) {\n  if (!graph || !Array.isArray(graph.nodes) || !Array.isArray(graph.edges)) throw new Error('graph must contain nodes[] and edges[]')\n  if (!Array.isArray(cases) || cases.length === 0) throw new Error('cases must be a non-empty array')\n  const semanticResults = evaluateCases(graph, cases)\n  const trustedGraph = withoutUnsupportedNodes(graph)\n  const trustedResults = evaluateCases(trustedGraph, cases)\n  const semantic = aggregateResults(semanticResults)\n  const trusted = aggregateResults(trustedResults)\n  const excludedUnsupportedNodeIds = (graph.nodes || []).filter((node) => node && node.groundingStatus === 'unsupported').map((node) => node.id)\n  return {\n    ok: semantic.passed === semantic.total && trusted.passed === trusted.total,\n    passed: trusted.passed,\n    total: trusted.total,\n    score: trusted.score,\n    semanticPassed: semantic.passed,\n    trustedPassed: trusted.passed,\n    semanticScore: semantic.score,\n    trustedScore: trusted.score,\n    categories: trusted.categories,\n    semanticCategories: semantic.categories,\n    trustedCategories: trusted.categories,\n    excludedUnsupportedNodeIds,\n    results: trustedResults,\n    semanticResults,\n  }\n}`
benchmark = replaceOnce(benchmark, oldRun, newRun, 'QA benchmark aggregation')
writeFileSync(benchmarkPath, benchmark)

// ---- QA fixture: semantic slots instead of whole-sentence whack-a-mole ----
const fixturePath = 'scripts/fixtures/preface-qa-cases.json'
let fixture = readFileSync(fixturePath, 'utf8')
const oldMethods = `      "all": [\n        ["手段本身并非有问题", "方法本身并非有问题", "并非这些手段有问题", "并非这些方法有问题", "并非因为这些手段本身有问题", "并非因为这些方法本身有问题"]\n      ]`
const newMethods = `      "all": [\n        ["手段", "方法"],\n        ["并非", "不是", "没有", "并不在于"],\n        ["本身有问题", "本身就是问题"]\n      ]`
fixture = replaceOnce(fixture, oldMethods, newMethods, 'methods semantic slots')
writeFileSync(fixturePath, fixture)

// ---- QA smoke: unsupported nodes must not earn trusted answers ----
const qaSmokePath = 'scripts/kg-qa-benchmark-smoke.mjs'
let qaSmoke = readFileSync(qaSmokePath, 'utf8')
const qaOldTail = `assert(result.ok, 'benchmark fixture did not fully pass: ' + JSON.stringify(result.results.filter((r) => !r.pass)))\nassert(result.score === 100 && result.total === 7, 'benchmark aggregate score is wrong')\nassert(result.categories.answerability.passed === 3, 'answerability metric is wrong')\nassert(result.categories['unknown-calibration'].passed === 1, 'unknown calibration metric is wrong')\nassert(result.results.find((r) => r.id === 'path').edgeEvidence.includes('progress>relearn:causes'), 'faithful path evidence is missing')\nassert(result.results.find((r) => r.id === 'atomic-or-path').matchedKind === 'node', 'atomic proposition was not accepted as alternative evidence')\nassert(result.results.find((r) => r.id === 'unknown').verdict === 'insufficient', 'unknown answer was not calibrated as insufficient')\nassert(result.results.find((r) => r.id === 'unknown').evidence.includes('goal'), 'unknown calibration did not preserve the topic anchor as evidence')\nconsole.log(JSON.stringify({ ok: true, score: result.score, categories: result.categories }))`
const qaNewTail = `assert(result.ok, 'benchmark fixture did not fully pass: ' + JSON.stringify(result.results.filter((r) => !r.pass)))\nassert(result.score === 100 && result.semanticScore === 100 && result.trustedScore === 100 && result.total === 7, 'benchmark aggregate score is wrong')\nassert(result.categories.answerability.passed === 3, 'answerability metric is wrong')\nassert(result.categories['unknown-calibration'].passed === 1, 'unknown calibration metric is wrong')\nassert(result.results.find((r) => r.id === 'path').edgeEvidence.includes('progress>relearn:causes'), 'faithful path evidence is missing')\nassert(result.results.find((r) => r.id === 'atomic-or-path').matchedKind === 'node', 'atomic proposition was not accepted as alternative evidence')\nassert(result.results.find((r) => r.id === 'unknown').verdict === 'insufficient', 'unknown answer was not calibrated as insufficient')\nassert(result.results.find((r) => r.id === 'unknown').evidence.includes('goal'), 'unknown calibration did not preserve the topic anchor as evidence')\n\nconst unsupportedGraph = {\n  ...graph,\n  nodes: graph.nodes.map((node) => node.id === 'relearn' ? { ...node, groundingStatus: 'unsupported' } : node),\n}\nconst trustResult = runGraphQaBenchmark(unsupportedGraph, cases)\nassert(trustResult.semanticScore === 100, 'semantic score should still describe the full graph')\nassert(trustResult.trustedScore < 100 && trustResult.score === trustResult.trustedScore, 'unsupported node still earned trusted benchmark credit')\nassert(trustResult.semanticResults.find((r) => r.id === 'path').pass, 'semantic result unexpectedly lost the unsupported path')\nassert(!trustResult.results.find((r) => r.id === 'path').pass, 'trusted result used an unsupported path endpoint')\nassert(trustResult.excludedUnsupportedNodeIds.includes('relearn'), 'benchmark did not report excluded unsupported evidence')\nassert(!trustResult.ok, 'benchmark should not be green when trusted evidence fails')\nconsole.log(JSON.stringify({ ok: true, score: result.score, semanticScore: result.semanticScore, trustedScore: result.trustedScore, categories: result.categories }))`
qaSmoke = replaceOnce(qaSmoke, qaOldTail, qaNewTail, 'QA trust smoke')
writeFileSync(qaSmokePath, qaSmoke)

// ---- Typography-only evidence regression ----
const typographySmoke = `import hostPlugin from '../src/index.host.js'\n\nfunction assert(condition, message) {\n  if (!condition) throw new Error(message)\n}\n\nconst handlers = new Map()\nglobalThis.harness = { handle(name, handler) { handlers.set(name, handler) } }\n\nlet mode = 'unique'\nconst extractor = {\n  async extractChunk() {\n    if (mode === 'unique') {\n      return {\n        summary: 'quote typography',\n        nodes: [\n          { id: 'n1', type: 'concept', text: '感觉懂了', quote: '“感觉懂了”', paragraph: 0 },\n          { id: 'n2', type: 'concept', text: '行为目标', quote: '行为目标', paragraph: 0 },\n        ],\n        edges: [\n          { fromNodeId: 'n1', toNodeId: 'n2', relation: 'not_is', evidence: [{ paragraph: 0, quote: '“感觉懂了”并不是明确的行为目标' }] },\n        ],\n      }\n    }\n    return {\n      summary: 'ambiguous quote typography',\n      nodes: [{ id: 'n1', type: 'concept', text: '感觉懂了', quote: '“感觉懂了”', paragraph: 0 }],\n      edges: [],\n    }\n  },\n}\n\nhostPlugin().apply({\n  get(name) { return name === 'kgExtractor' ? extractor : null },\n  interval() { return () => {} },\n})\n\nasync function waitTask(taskId) {\n  for (let i = 0; i < 240; i += 1) {\n    const status = await handlers.get('task-status')({ taskId })\n    if (status.status !== 'running') return status\n    await new Promise((resolve) => setTimeout(resolve, 5))\n  }\n  throw new Error('task did not finish')\n}\n\nconst uniqueSource = '"感觉懂了"并不是明确的行为目标。'\nconst uniqueStarted = await handlers.get('extract')({ title: 'typography-unique', text: uniqueSource })\nconst uniqueDone = await waitTask(uniqueStarted.taskId)\nassert(uniqueDone.status === 'succeeded', 'unique typography extraction failed: ' + JSON.stringify(uniqueDone))\nconst uniqueNode = uniqueDone.result.nodes.find((node) => node.id === 'n1')\nconst uniqueEdge = uniqueDone.result.edges.find((edge) => edge.fromNodeId === 'n1' && edge.toNodeId === 'n2' && edge.relation === 'not_is')\nassert(uniqueNode && uniqueNode.groundingStatus === 'grounded', 'unique typography drift did not become grounded')\nassert(uniqueNode.quote === '"感觉懂了"', 'node quote was not rebound to the exact source spelling: ' + JSON.stringify(uniqueNode && uniqueNode.quote))\nassert(uniqueNode.evidence.some((item) => item.quote === '"感觉懂了"'), 'node evidence did not preserve exact source quote')\nassert(uniqueEdge && uniqueEdge.evidence.some((item) => item.quote === '"感觉懂了"并不是明确的行为目标'), 'relation evidence was dropped instead of uniquely rebound')\n\nmode = 'ambiguous'\nconst ambiguousSource = '"感觉懂了"不是目标；再次说"感觉懂了"也不是目标。'\nconst ambiguousStarted = await handlers.get('extract')({ title: 'typography-ambiguous', text: ambiguousSource })\nconst ambiguousDone = await waitTask(ambiguousStarted.taskId)\nassert(ambiguousDone.status === 'succeeded', 'ambiguous typography extraction failed: ' + JSON.stringify(ambiguousDone))\nconst ambiguousNode = ambiguousDone.result.nodes.find((node) => node.id === 'n1')\nassert(ambiguousNode && ambiguousNode.groundingStatus === 'unsupported', 'ambiguous typography match was incorrectly authenticated')\nassert(Array.isArray(ambiguousNode.evidence) && ambiguousNode.evidence.length === 0, 'ambiguous typography match received evidence')\n\nconsole.log(JSON.stringify({ ok: true, uniqueRebound: true, ambiguousFailClosed: true }))\n`
writeFileSync('scripts/kg-typographic-evidence-smoke.mjs', typographySmoke)

// ---- package test wiring ----
const packagePath = 'package.json'
let pkg = readFileSync(packagePath, 'utf8')
pkg = replaceOnce(pkg,
  `    "test:kg-evidence": "node scripts/kg-evidence-provenance-smoke.mjs",\n`,
  `    "test:kg-evidence": "node scripts/kg-evidence-provenance-smoke.mjs",\n    "test:kg-typographic-evidence": "node scripts/kg-typographic-evidence-smoke.mjs",\n`,
  'typographic evidence test script')
pkg = replaceOnce(pkg,
  `npm run test:kg-evidence && npm run test:kg-generation-gate`,
  `npm run test:kg-evidence && npm run test:kg-typographic-evidence && npm run test:kg-generation-gate`,
  'typographic evidence test aggregate')
writeFileSync(packagePath, pkg)
